{
  "name": "commit-standardizer",
  "author": "pedrolopes-ilv",
  "version": "1.0.0",
  "description": "A package that helps you to keep your commit history standardized",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo 'TESTE'",
    "prebuild": "rm -rf dist/",
    "commit": "node scripts/commit.js",
    "build": "rollup -c",
    "generate:docs": "typedoc --mode file --out ./doc ./index.js --allowJs --ignoreCompilerErrors --module commonjs --includeDeclarations --excludeExternals --hideGenerator"
  },
  "files": [
    "dist/index.js"
  ],
  "keywords": [
    "commits",
    "standard",
    "semantic"
  ],
  "license": "MIT",
  "dependencies": {
    "prompts": "^2.1.0"
  },
  "devDependencies": {
    "rollup-plugin-terser": "^5.1.1",
    "typedoc": "^0.14.2"
  }
}
